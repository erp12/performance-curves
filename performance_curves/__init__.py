"""Write me."""
from typing import List


def foo(i: int, s: str) -> List[str]:
    """A simple function

    Arguments:
        i: Some number
        s: Some test

    Return:
        A list of `s` with `i` elements.

    """
    return [s] * i
